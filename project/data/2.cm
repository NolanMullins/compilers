/* 
 * This program introduces function calls
 * No Errors
 * Difficulty: Easy
 */

void add (int x, int y) {
    int res;
    res = x + y;
    return res;
}

void main(void) {
    int x; 
    int y;
    int z;
    x = add(1, 1);
    y = add(5, 0);
    z = add(x, y);
    output(z);
}